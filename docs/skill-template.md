# Skill Template

Every skill should ship with:
- `manifest.yaml`
- `schemas/input.schema.json` + `schemas/output.schema.json`
- sandbox definition (`sandbox/Dockerfile`, `sandbox/permissions.yaml`)
- `README.md` (usage + limitations)
- tests (`tests/unit`, `tests/golden`)
